{
  "dependencies": {
    "bootstrap": "^5.2.3"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.3",
    "parcel": "^2.8.3"
  },
  "scripts": {
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html",
    "build-public": "parcel build src/index.html --dist-dir build/public",
    "clean-build-public": "rm -rf build/public"
  }
}
